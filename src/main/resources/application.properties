spring.application.name=tictactoe

spring.cassandra.contact-points=${CASSANDRA_HOST:127.0.0.1}
spring.cassandra.port=${CASSANDRA_PORT:9042}
spring.cassandra.keyspace-name=${CASSANDRA_KEYSPACE:tictactoe}
spring.cassandra.local-datacenter=${CASSANDRA_DATACENTER:datacenter1}
